import 'package:flutter/material.dart';

class AiMentorScreen extends StatelessWidget {
  const AiMentorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xFF241445),
      body: SafeArea(
        child: Container(
          width: 394,
          height: 882,
          decoration: BoxDecoration(color: const Color(0xFF241445)),
          child: Stack(
              children: [
                  // ... (original content)
              ],
          ),
        ),
      ),
    );
  }
}